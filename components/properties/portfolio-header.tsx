import { PageHeader } from "@/components/layout/page-header"

export function PortfolioHeader() {
  return (
    <PageHeader
      title="Portfolio"
      description="Manage active listings, monitor inventory, and match properties with buyers."
    />
  )
}